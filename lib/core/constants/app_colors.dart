import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF79AEDB);
  static const accent = Color(0xFF00C896);

  static const lowRisk = Colors.green;
  static const mediumRisk = Colors.orange;
  static const highRisk = Colors.red;
}